[package]
name = "vm"
version = "0.1.0"
edition = "2021"

[dependencies]
bincode = { workspace = true }
fhe = { path = "../fhe" }
winterfell = { version = "0.9.0" }

[dev-dependencies]
tempfile = { version = "3.12.0" }